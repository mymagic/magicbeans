<nav class="navbar navbar-default navbar-fixed-top" role="navigation">
  <div class="container">
	<div class="navbar-header">
	  <button type="button" class="navbar-toggle collapsed" data-toggle="collapse" data-target="#navbar" aria-expanded="false" aria-controls="navbar">
		<span class="sr-only">Toggle navigation</span>
		<span class="icon-bar"></span>
		<span class="icon-bar"></span>
		<span class="icon-bar"></span>
	  </button>
	  <a class="navbar-brand" href="/">Magicbeans</a>
	</div>
	<div id="navbar" class="collapse navbar-collapse">
	  <ul class="nav navbar-nav">
		<li <%= current_page?(root_path)? "class=active" : "" %> ><a href="<%= root_path %>">Home</a></li>
		<li <%= current_page?(users_path)? "class=active" : "" %>><a href="<%= users_path %>">Users</a></li>
	  </ul>
	  
	  <ul class="nav navbar-nav navbar-right">
		    <% 
			  if user_signed_in?
			  	%>
			  	<%
				  the_link = link_to 'Sign Out', destroy_user_session_path, {method: :delete}
			  else
				  the_link = link_to 'Sign In', new_user_session_path
				  sign_up = link_to 'Sign Up', new_user_registration_path
			%>	  
                  <li <%= current_page?(new_user_registration_path)? "class=active" : "" %> ><%= sign_up %></li>
                  <li class="divider"></li>
			<%
			  end
            %>
		  	<li <%= current_page?(new_user_session_path) ? "class=active" : "" %> ><%=  the_link %></li>
	  </ul>
	</div><!--/.nav-collapse -->
  </div>
</nav>